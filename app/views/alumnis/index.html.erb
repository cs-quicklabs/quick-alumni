<% content_for :title, "Alumnis" %>
<%= render "shared/nav"%>

<section class="bg-gray-50 dark:bg-gray-900 py-3 sm:py-5 min-h-screen">
  <div class="px-4 mx-auto max-w-screen-2xl lg:px-8">
    <div class="relative overflow-hidden bg-white shadow-md dark:bg-gray-800 sm:rounded-sm">
      <div class=" divide-y dark:divide-gray-700">
        <div class="flex-row items-center justify-between p-4 space-y-3 sm:flex sm:space-y-0 sm:space-x-4">
          <div>
            <h1 class="mr-3 text-lg font-semibold dark:text-white">Alumni</h1>
            <p class="text-gray-500 dark:text-gray-400 text-sm">Manage all your past team members or add a new alumni.</p>
          </div>
          <a type="button" href="<%= new_alumni_path %>"
                class="btn-primary">
            Add new alumni
          </a>
        </div>
      </div>
      <div class="overflow-x-auto">
        <table class="w-full text-sm text-left text-gray-500 dark:text-gray-400">
          <thead class="text-xs text-gray-700 uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
            <tr>
              <th scope="col" class="px-4 py-3">Name</th>
              <th scope="col" class="px-4 py-3">Email</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-gray-200">
            <%= render partial: "alumni", collection: @alumnis, as: :alumni %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>
